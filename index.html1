<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Indian Job Board</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    *{
      margin:0; padding:0; box-sizing:border-box;
      font-family:'Poppins', sans-serif;
    }

    :root {
      --primary: #ff671f;
      --secondary: #046a38;
      --background:#f4f6fa;
      --card:#ffffff;
      --text:#222;
      --gray:#777;
      --radius: 18px;
    }

    body{
      background:linear-gradient(135deg, var(--primary), var(--secondary));
      min-height:100vh;
      padding:1.2rem;
    }

    header{
      background:var(--card);
      padding:2rem 1.5rem;
      border-radius:var(--radius);
      text-align:center;
      box-shadow:0 5px 20px rgba(0,0,0,0.15);
      margin-bottom:2rem;
      animation:fadeDown .7s ease;
    }

    header h1{
      font-size:2.3rem; 
      font-weight:700;
      color:var(--secondary);
    }
    header p{
      margin-top:.5rem;
      color:var(--text);
      font-size:1.1rem;
    }

    /* Search Bar */
    .search-box{
      margin-top:1.4rem;
      display:flex;
      gap:.7rem;
      justify-content:center;
      flex-wrap:wrap;
    }
    .search-box input{
      width:250px;
      padding:.8rem 1rem;
      border:1px solid #ddd;
      border-radius:10px;
      font-size:1rem;
      background:#fff;
    }
    .search-box button{
      padding:.8rem 1.3rem;
      border:none;
      font-size:1rem;
      background:var(--secondary);
      color:#fff;
      border-radius:10px;
      font-weight:600;
      cursor:pointer;
      transition:.2s;
    }
    .search-box button:hover{
      background:var(--primary);
      transform:scale(1.05);
    }

    /* Job Cards */
    .jobs{
      display:grid;
      gap:1.5rem;
      margin-top:1.2rem;
    }

    .job-card{
      background:var(--card);
      padding:1.5rem;
      border-radius:var(--radius);
      box-shadow:0 5px 20px rgba(0,0,0,0.1);
      border-left:8px solid var(--primary);
      animation:fadeUp .7s ease;
      transition:.3s;
    }
    .job-card:hover{
      transform:translateY(-4px);
      border-left:8px solid var(--secondary);
    }

    .job-title{
      font-size:1.35rem;
      font-weight:600;
      color:var(--primary);
      margin-bottom:6px;
    }
    .job-info{
      font-size:.95rem;
      color:var(--text);
      margin-bottom:6px;
    }
    .job-location{
      color:var(--secondary);
      font-weight:600;
    }

    .apply-btn{
      margin-top:1rem;
      padding:.7rem 1.2rem;
      background:linear-gradient(90deg, var(--secondary), var(--primary));
      border:none;
      color:#fff;
      border-radius:10px;
      font-size:1rem;
      cursor:pointer;
      font-weight:600;
      transition:.2s;
    }
    .apply-btn:hover{
      transform:scale(1.05);
    }

    @media(min-width:700px){
      .jobs{
        grid-template-columns:repeat(2, 1fr);
      }
    }

    @keyframes fadeDown{
      from{opacity:0; transform:translateY(-20px);}
      to{opacity:1; transform:translateY(0);}
    }
    @keyframes fadeUp{
      from{opacity:0; transform:translateY(20px);}
      to{opacity:1; transform:translateY(0);}
    }

    footer{
      text-align:center;
      color:#fff;
      margin-top:2rem;
      font-size:.95rem;
      opacity:.9;
    }

  </style>
</head>
<body>

  <header>
    <h1>🇮🇳 Indian Job Board</h1>
    <p>Find the latest jobs across India instantly.</p>

    <div class="search-box">
      <input type="text" id="searchInput" placeholder="Search job (ex: Developer, Teacher)">
      <button onclick="searchJobs()">Search</button>
    </div>
  </header>

  <section class="jobs" id="jobList">

    <div class="job-card">
      <div class="job-title">Frontend Developer</div>
      <div class="job-info"><strong>Company:</strong> Infosys</div>
      <div class="job-location">Bangalore • Full Time</div>
      <div class="job-info">Build UI for web apps using HTML, CSS, and JS.</div>
      <button class="apply-btn">Apply Now</button>
    </div>

    <div class="job-card">
      <div class="job-title">Graphic Designer</div>
      <div class="job-info"><strong>Company:</strong> Wipro</div>
      <div class="job-location">Hyderabad • Remote</div>
      <div class="job-info">Adobe Photoshop, Illustrator, UI Concepts.</div>
      <button class="apply-btn">Apply Now</button>
    </div>

    <div class="job-card">
      <div class="job-title">Data Analyst</div>
      <div class="job-info"><strong>Company:</strong> TCS</div>
      <div class="job-location">Mumbai • Full Time</div>
      <div class="job-info">SQL, Excel, Power BI & dashboards.</div>
      <button class="apply-btn">Apply Now</button>
    </div>

    <div class="job-card">
      <div class="job-title">Teacher (Science)</div>
      <div class="job-info"><strong>Company:</strong> Delhi Public School</div>
      <div class="job-location">Delhi • Full Time</div>
      <div class="job-info">Teach science subjects for 8th–10th grade.</div>
      <button class="apply-btn">Apply Now</button>
    </div>

  </section>

  <footer>© 2025 Indian Job Board — Made with ❤️</footer>

<script>
  function searchJobs(){
    let input = document.getElementById("searchInput").value.toLowerCase();
    let cards = document.querySelectorAll(".job-card");

    cards.forEach(card=>{
      let title = card.querySelector(".job-title").textContent.toLowerCase();
      card.style.display = title.includes(input) ? "block" : "none";
    });
  }
</script>

</body>
</html>
